# Next.js ランタイム（app-prod）へのリバースプロキシ用 Nginx イメージ
FROM nginx:1.25-alpine

# テンプレートとして配置（起動時に envsubst で変数展開される）
COPY nginx.conf /etc/nginx/templates/default.conf.template

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
